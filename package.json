{
  "name": "anthony-test",
  "version": "1.0.0",
  "description": "anthony-test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "sass .:. --watch --no-source-map --style=compressed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "1.14.0"
  }
}
